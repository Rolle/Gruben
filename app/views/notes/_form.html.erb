<%= tinymce_assets %>
<%= form_for(@note) do |f| %>
  <div class="form-group">
    <%= f.label "Notizen:", :class => "form-label" %>
    <div class="controls">
    	<%= f.text_area :notice, :class => "tinymce form-control", :rows => 40, :cols => 70 %>
    </div>
  </div>
  <div class="form-group">
    <%= f.submit "Speichern", :class=>"btn btn-primary" %>
  </div>
<% end %>
<%= tinymce %>