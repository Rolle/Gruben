<%= tinymce_assets %>
<%= form_for(@note) do |f| %>
  <div class="control-group">
    <%= f.label "Notizen:", :class => "control-label" %>
    <div class="controls">
      <%= text_area_tag :notice, "", :class => "tinymce", :rows => 40, :cols => 70 %>

    </div>
  </div>
  <div class="actions">
    <%= f.submit "Speichern", :class=>"btn btn-primary" %>
  </div>
<% end %>
<%= tinymce %>