<%= form_for(@note) do |f| %>
  <div class="control-group">
    <%= f.label "Notizen:", :class => "control-label" %>
    <div class="controls">
      <%= f.cktext_area :notice, :toolbar => 'Basic' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit "Speichern", :class=>"btn btn-primary" %>
  </div>
<% end %>
